>`tree.js`: initial attempt at loading the pageGoblin-emitted, flat `map.json` into nested data at runtime 
>`createNavFromSitemap.js`: first attempt at doing `map.json`->`loadUrlTree`->`produce nav menu` for the LF header style
>`populateMenu.js`: second attempt, doing `map.json`->`loadUrlTree`->`produce nav menu` for the menu3 style
